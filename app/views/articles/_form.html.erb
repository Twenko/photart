<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    Titre<br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    Contenu<br />
    <%= f.text_area :content %>
  </div>
  <div class="field">
    Gallerie attribuée pour l'article<br />
    <%= f.number_field :gallery_id %>
  </div>
  <div class="field">
    Photo attribuée pour l'article<br />
    <%= f.number_field :picture_id %>
  </div>
  <div class="actions">
    <%= f.submit "Valider" %>
  </div>
<% end %>
